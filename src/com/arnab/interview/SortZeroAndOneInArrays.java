package com.arnab.interview;

public class SortZeroAndOneInArrays {

    public static void main(String[] args) {




       /* for(int i=0; i<=arr.length-1; i++) {
            int a = arr[i];
            int b = arr[i+1];
            if(arr[i]>arr[i+1]) {
                arr[i] = arr[i]+arr[i+1];
                arr[i+1] = arr[i]-arr[i+1];
                arr[i] = arr[i]-arr[i+1];
            }
        }
        Arrays.stream(arr).forEach(
                a -> System.out.println(a)
        );*/


    }
}
